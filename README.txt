# Author: github.com/brendansturges
# Description: getSite.ps1 - This script queries a file the user chooses and checks all servers within to see which sccm site this server is connected to and outputs that info to a file that the user specifies.
# Requires SCCM